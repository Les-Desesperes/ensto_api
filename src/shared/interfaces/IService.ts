/**
 * Base interface for all services.
 * Defines the contract that all service classes must follow.
 */
export interface IService {
    // Placeholder for common service methods if needed
    // Can be extended by specific service interfaces
}

